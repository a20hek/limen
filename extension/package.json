{
  "name": "@limen/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "pack": "mkdir -p dist && zip -r dist/limen-extension.zip manifest.json background.js content.js README.md"
  }
}
